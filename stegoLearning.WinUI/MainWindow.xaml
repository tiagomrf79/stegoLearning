<Window
    x:Class="stegoLearning.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:stegoLearning.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!--cabeçalho-->
        <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Background="Orange">
            <StackPanel>
                <TextBlock Text="stegoLearning" FontSize="40" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!--painel de navegação-->
        <Border Grid.Row="1" Grid.Column="0" Background="LightBlue">
            <StackPanel>
                <TextBlock Text="MENU DE NAVEGAÇÃO"/>
            </StackPanel>
        </Border>

        <!--formulário-->
        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <!--formulário de encriptação-->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <TextBox Grid.Row="0" x:Name="sendPlainText" Header="Mensagem a enviar" Margin="10"/>
                <TextBox Grid.Row="1" x:Name="senderKey" Header="Chave para encriptação" Margin="10"/>
                <Button Grid.Row="2" x:Name="encrypt" Content="Encriptar mensagem" Margin="10" Click="encrypt_Click"/>
                <TextBox Grid.Row="3" x:Name="sendCypherText" Header="Mensagem encriptada" Margin="10"/>
                <Button Grid.Row="4" x:Name="send" Content="Enviar" Margin="10" VerticalAlignment="Top" Click="send_Click"/>
            </Grid>

            <!--formulário de desencriptação-->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBox Grid.Row="0" x:Name="receivedCypherText" Header="Mensagem encriptada" Margin="10"/>
                <TextBox Grid.Row="1" x:Name="receiverKey" Header="Chave para desencriptação" Margin="10"/>
                <Button Grid.Row="2" x:Name="decrypt" Content="Desencriptar mensagem" Margin="10" Click="decrypt_Click"/>
                <TextBox Grid.Row="3" x:Name="receivedPlainText" Header="Mensagem recebida" Margin="10"/>
            </Grid>
        </Grid>
    </Grid>

        

</Window>
